lbl printf
    ;; print's a string where memory address is in RBX
    out RAX $RBX
    ret

lbl printchar
    ;; print's a character in RBX
    cout RAX RBX
    ret

lbl printint
    ;; print's an integer in RBX
    out RAX RBX
    ret
